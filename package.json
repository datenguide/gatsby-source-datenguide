{
  "name": "gatsby-source-datenguide",
  "version": "1.0.3",
  "description": "Gatsby source plugin for building websites using the datenguide API as a data source",
  "scripts": {
    "build": "babel src --out-dir . --ignore __tests__",
    "watch": "babel -w src --out-dir . --ignore __tests__",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "gatsby-source-plugin"
  ],
  "author": "Simon Jockers",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.16.1",
    "babel-runtime": "^6.26.0",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "cross-env": "^5.0.5"
  }
}
